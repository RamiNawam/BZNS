================================================================
BZNS — RAMI'S BACKEND CHANGES LOG
Claude Builders Hackathon @ McGill — April 1-4, 2026
================================================================
Owner: Rami Nawam
Role: Backend / Database / API / Supabase
Last updated: April 1, 2026
================================================================


----------------------------------------------------------------
DATABASE CREATION DOCUMENTATION
----------------------------------------------------------------

DATE: April 1, 2026
ACTION: Initial Supabase database schema setup
PROJECT: RamiNawam's Project (ID: ncpmghgxtaqhzohauymn, region: us-east-1)

----------------------------------------------------------------

OVERVIEW
--------
Connected to the existing Supabase project and applied the full
initial database schema for the BZNS micro-business launchpad
application. This covers all tables, relationships, security
policies, indexes, and storage needed for the app to function.

----------------------------------------------------------------

TABLES CREATED
--------------

1. profiles
   - The central table. One row per user.
   - Stores everything from the intake wizard: business name,
     type, sector, location, borough, home-based flag, age,
     immigration status, gender, languages, preferred language,
     business structure, partner/employee info, expected monthly
     revenue, startup budget, NEQ/GST status.
   - Also stores financial snapshot inputs: monthly_expenses,
     expense_categories (JSONB), price_per_unit, units_per_month.
   - intake_completed flag tracks whether user finished onboarding.
   - intake_answers (JSONB) stores raw answers for reference.
   - RLS: ON — users can only access their own row.
   - Foreign key: user_id → auth.users(id), CASCADE DELETE.

2. roadmap_steps
   - Stores the AI-generated legal roadmap per user.
   - Each row is one step: step_order, step_key, title,
     description, why_needed, estimated_cost, estimated_timeline,
     required_documents (TEXT[]), government_url, source,
     depends_on (TEXT[]).
   - status field: 'pending' | 'in_progress' | 'completed' | 'skipped'
   - completed_at timestamp + notes field for user tracking.
   - UNIQUE(profile_id, step_key) — no duplicate steps per user.
   - RLS: ON — via profile_id → profiles → auth.uid() check.
   - Foreign key: profile_id → profiles(id), CASCADE DELETE.

3. funding_matches
   - Stores the deterministic funding scorer results per user.
   - Each row is one program: program_key, program_name,
     program_type (loan/grant/tax_credit/mentorship),
     amount_description, match_score (0-100),
     eligibility_details (JSONB), summary, application_url,
     source_url.
   - is_bookmarked and is_dismissed flags for user interaction.
   - UNIQUE(profile_id, program_key) — one score per program per user.
   - RLS: ON — via profile_id → profiles → auth.uid() check.
   - Foreign key: profile_id → profiles(id), CASCADE DELETE.

4. financial_snapshots
   - Stores the full output of the hybrid tax calculator + Claude insights.
   - Inputs stored: gross_monthly_revenue, monthly_expenses, business_structure.
   - Deterministic calc outputs: annual_revenue, gst_collected,
     qst_collected, gst_qst_remittance, net_revenue,
     federal_income_tax, provincial_income_tax, qpp_contribution,
     qpip_premium, total_deductions, monthly_take_home,
     effective_take_home_rate, quarterly_installment.
   - Claude insight outputs (JSONB): suggested_expenses,
     watch_out_flags. Text: pricing_insight.
   - UNIQUE(profile_id) — one snapshot per user (upserted on recalc).
   - RLS: ON — via profile_id → profiles → auth.uid() check.
   - Foreign key: profile_id → profiles(id), CASCADE DELETE.

5. chat_messages
   - Stores full conversation history per user.
   - role: 'user' | 'assistant'
   - content: message text
   - sources (TEXT[]): KB files cited by Claude
   - context_type: which screen the message was sent from
   - session_id (UUID): groups messages into sessions
   - RLS: ON — via profile_id → profiles → auth.uid() check.
   - Foreign key: profile_id → profiles(id), CASCADE DELETE.

6. generated_documents  [STRETCH FEATURE]
   - Stores metadata for .docx files generated by the Starter Kit.
   - document_type, file_name, storage_path, download_url, metadata (JSONB).
   - Actual files live in Supabase Storage (documents bucket).
   - RLS: ON — via profile_id → profiles → auth.uid() check.
   - Foreign key: profile_id → profiles(id), CASCADE DELETE.

7. response_cache
   - Stores pre-generated Claude responses for demo day.
   - cache_key (TEXT, UNIQUE): identifier for the cached response.
   - response_data (JSONB): the full cached payload.
   - expires_at: auto-set to 24 hours after creation.
   - No RLS — system-level table, not user-specific.

----------------------------------------------------------------

FOREIGN KEY CHAIN / RELATIONSHIPS
----------------------------------

auth.users (Supabase built-in)
    └── profiles             (user_id → auth.users.id)
            ├── roadmap_steps        (profile_id → profiles.id)
            ├── funding_matches      (profile_id → profiles.id)
            ├── financial_snapshots  (profile_id → profiles.id)
            ├── chat_messages        (profile_id → profiles.id)
            └── generated_documents  (profile_id → profiles.id)

All foreign keys use ON DELETE CASCADE — deleting a user
removes all their data automatically.

----------------------------------------------------------------

ROW LEVEL SECURITY (RLS) POLICIES
-----------------------------------

Applied to: profiles, roadmap_steps, funding_matches,
            financial_snapshots, chat_messages, generated_documents.

Pattern used:
  - profiles: FOR ALL USING (auth.uid() = user_id)
  - child tables: FOR ALL USING (
        profile_id IN (
            SELECT id FROM profiles WHERE user_id = auth.uid()
        )
    )

response_cache has NO RLS — it is a server-side system table
and is never accessed directly by the client.

----------------------------------------------------------------

PERFORMANCE INDEXES CREATED (8 total)
---------------------------------------

idx_profiles_user_id          → profiles(user_id)
idx_roadmap_profile           → roadmap_steps(profile_id)
idx_roadmap_order             → roadmap_steps(profile_id, step_order)
idx_funding_profile           → funding_matches(profile_id)
idx_funding_score             → funding_matches(profile_id, match_score DESC)
idx_chat_profile              → chat_messages(profile_id, created_at)
idx_snapshot_profile          → financial_snapshots(profile_id)
idx_cache_key                 → response_cache(cache_key)

----------------------------------------------------------------

STORAGE
--------

Bucket created: "documents" (private, not public)

Access policy:
  - Users can only access files inside their own folder.
  - Folder structure enforced: /user_id/filename.docx
  - Policy: bucket_id = 'documents' AND
            (storage.foldername(name))[1] = auth.uid()::text

----------------------------------------------------------------

NOTES / THINGS TO BE AWARE OF
-------------------------------

- The Supabase project already had pre-existing tables from
  another project (clubs, events, players, teams, users).
  These were NOT touched. All BZNS tables were added cleanly.

- The migration was applied as "initial_schema" and is tracked
  in Supabase's migration history.

- The local migration file at supabase/migrations/001_initial_schema.sql
  is a simplified version — the production schema applied directly
  to Supabase is the authoritative one documented here.

----------------------------------------------------------------

NEXT STEPS (backend)
----------------------

[x] Wire up Supabase client in src/lib/supabase/client.ts and server.ts
[x] Implement POST /api/profile route
[x] Implement POST /api/roadmap route
[x] Implement POST /api/funding route with scorer.ts
[x] Implement POST /api/financial-snapshot — tax calculator + Claude
[x] Implement POST /api/assistant route
[ ] Seed demo account (Yara) for hackathon day
[ ] Pre-populate response_cache for demo account

================================================================
END OF ENTRY — April 1, 2026
================================================================


================================================================
BACKEND ARCHITECTURE + INTAKE WIZARD — SESSION 2
================================================================

DATE: April 1, 2026
BRANCH: feat/backend-logic
BUILD STATUS: ✅ Zero TypeScript errors (Next.js 14.2.35)

----------------------------------------------------------------

OVERVIEW
--------
Completed the full backend layer architecture, fixed the intake
wizard redirect loop, rewrote all intake questions, and migrated
every component and service file to use the correct snake_case
types that match the Supabase schema.

----------------------------------------------------------------

1. LAYERED BACKEND ARCHITECTURE
--------------------------------

Implemented a Spring Boot-style layered architecture across the
entire app. Each feature follows the same chain:

  API Route (Controller)
       ↓
  Service (business logic)
       ↓
  Repository (Supabase queries)
       ↓
  Types (DB-mirrored interfaces)

FILES CREATED:

Repositories (raw Supabase access — no business logic):
  src/repositories/profile.repository.ts
  src/repositories/roadmap.repository.ts
  src/repositories/funding.repository.ts
  src/repositories/snapshot.repository.ts
  src/repositories/chat.repository.ts
  src/repositories/cache.repository.ts

Services (business logic, orchestration):
  src/services/profile.service.ts
  src/services/roadmap.service.ts
  src/services/funding.service.ts
  src/services/financial.service.ts
  src/services/assistant.service.ts
  src/services/cache.service.ts

All 6 API routes updated to be thin controllers that delegate
to their respective service:
  src/app/api/profile/route.ts         → ProfileService
  src/app/api/roadmap/route.ts         → RoadmapService
  src/app/api/funding/route.ts         → FundingService
  src/app/api/financial-snapshot/route.ts → FinancialService
  src/app/api/assistant/route.ts       → AssistantService
  src/app/api/cache/route.ts           → CacheService

----------------------------------------------------------------

2. AUTHENTICATION FLOW
-----------------------

src/app/(auth)/login/page.tsx
  - Rewrote to remove magic link.
  - Mode toggle: Sign In / Create Account on same page.
  - Email + password: signInWithPassword() / signUp()
  - Google OAuth: signInWithOAuth({ provider: 'google' })
  - Real Google SVG icon (not an emoji).
  - emailRedirectTo points to /api/auth/callback.

src/app/api/auth/callback/route.ts  [NEW]
  - Server route that exchanges Supabase OAuth code for session cookie.
  - Used for both Google OAuth and email confirmation flows.
  - Redirects to /dashboard on success, /login on failure.

src/middleware.ts  [NEW]
  - Root Next.js middleware — runs on every single request.
  - Refreshes the Supabase session cookie (keeps it alive).
  - Redirects unauthenticated users to /login (with redirectTo param).
  - Redirects already-logged-in users away from /login → /dashboard.
  - NEW: Forwards current pathname as x-pathname request header
    so server-component layouts can read it without client-side tricks.

----------------------------------------------------------------

3. INTAKE WIZARD — REDIRECT LOOP FIX
--------------------------------------

PROBLEM:
  The intake page lived inside the (app) route group.
  The (app)/layout.tsx checked intake_completed and redirected
  to /intake if not done — but /intake was ALSO inside (app),
  causing an infinite redirect loop.

SOLUTION (no file deletion needed):
  - middleware.ts now sets header: x-pathname = current path
  - (app)/layout.tsx reads x-pathname via Next.js headers()
  - If user is on /intake AND intake is incomplete:
      → render children full-screen WITHOUT Sidebar/TopBar
      → clean onboarding UX, no redirect
  - If user is on any other page AND intake is incomplete:
      → redirect to /intake as before
  - If intake is complete:
      → render full app shell (Sidebar + TopBar) as normal

Files changed:
  src/middleware.ts                  — added x-pathname header forwarding
  src/app/(app)/layout.tsx           — reads header, handles intake path
  src/app/(app)/intake/page.tsx      — simplified to just <IntakeWizard />

----------------------------------------------------------------

4. INTAKE WIZARD — QUESTION CARD REWRITE
-----------------------------------------

src/components/intake/question-card.tsx  [FULL REWRITE]

Old: used ProfileFormData (old camelCase type), wrong questions,
     business structure / revenue projection / demographics checkboxes.

New: uses IntakeFormState (snake_case, matches store and DB).

STEP 0 — "Your Business"
  - Textarea: business_idea (free text, 4 rows)
  - Hint: "Be as specific or vague as you like"

STEP 1 — "Location & Setup"
  - Radio group: location (montreal / quebec_city / laval / other_quebec)
  - Conditional text input: borough (only shows if location = montreal)
  - Radio pair: is_home_based (Yes / No)

STEP 2 — "Finances"
  - Number input: expected_monthly_revenue ($ prefix, live annual equiv shown)
  - GST/QST $30K threshold hint shown dynamically
  - Radio pair: has_partners (Yes partnership / No solo founder)

STEP 3 — "About You"
  - Number input: age (optional, 16–99)
  - Radio group: immigration_status (5 options: citizen through prefer_not_to_say)
  - Toggle chips: languages (8 options: fr, en, es, ar, zh, pt, ht, other)
  - Radio pair: preferred_language (English / Français)

All UI uses teal colour scheme (no brand-* tokens that weren't defined).

----------------------------------------------------------------

5. TYPE SYSTEM MIGRATION — ALL COMPONENTS
------------------------------------------

Migrated every component and lib file from old camelCase types
to the new snake_case types that mirror Supabase columns.

Components fixed:
  src/components/financial/take-home-breakdown.tsx
    - grossIncome → gross_monthly_revenue (annual_revenue calc inline)
    - businessExpenses → monthly_expenses * 12
    - qpp/qpip/federalTax/quebecTax → snake_case equivalents
    - effectiveTaxRate → effective_take_home_rate

  src/components/financial/watch-out-flags.tsx
    - warnings: string[] → warnings: WatchOutFlag[] (with type/title/detail)
    - Now renders colour-coded cards: yellow=warning, blue=info, teal=tip

  src/components/financial/snapshot-card.tsx
    - snapshot.warnings → snapshot.watch_out_flags

  src/components/funding/funding-card.tsx  [REWRITE]
    - Removed dependency on nested match.program object
    - Uses flat FundingMatch fields: program_name, program_type,
      match_score, amount_description, eligibility_details,
      application_url, source_url
    - Inline expanded detail section (funding-detail.tsx retired)

  src/components/funding/funding-list.tsx
    - match.programId → match.program_key

  src/components/roadmap/roadmap-list.tsx
    - a.priority → a.step_order

  src/components/roadmap/roadmap-step.tsx  [REWRITE]
    - step.title_en → step.title
    - step.category/categoryColors → STATUS_BADGE map
    - step.estimatedTimeHours → step.estimated_timeline + estimated_cost
    - Uses teal colours throughout

  src/components/roadmap/step-detail.tsx  [REWRITE]
    - step.description_en → step.description (not shown, already in card)
    - step.links[] → step.why_needed + step.required_documents + step.government_url

  src/components/assistant/message-bubble.tsx
    - Props: ChatMessage (full DB type) → minimal inline interface
      (only id, role, content, sources? — the fields actually used)
    - brand-* colours → teal-*

  src/components/assistant/source-badge.tsx  [REWRITE]
    - source: SourceCitation → source: string (KB filename)
    - Auto-formats filename: "gst_qst.json" → "Gst Qst"

  src/stores/chat-store.ts
    - ChatMessage → ClientMessage (lightweight client-only type)
    - createdAt → created_at
    - Avoids requiring DB-only fields (profile_id, session_id, etc.)

Lib files fixed:
  src/lib/claude/prompts.ts
    - UserProfile → Profile (from @/types/profile)

  src/lib/funding/scorer.ts  [REWRITE]
    - UserProfile → Profile, FundingProgram → FundingProgramJSON
    - Returns CreateFundingMatchDTO (all snake_case DB fields)
    - Scores: age eligibility, location, immigration status, demographics
    - profile.city → profile.municipality
    - profile.languages → profile.languages_spoken

  src/services/funding.service.ts
    - Added UpdateFundingMatchDTO import
    - updateMatch() now typed with UpdateFundingMatchDTO

  src/types/chat.ts
    - Added SourceCitation interface { label: string; url?: string }

----------------------------------------------------------------

6. WHAT IS STILL TODO (Claude integration)
-------------------------------------------

The architecture is 100% wired and type-safe. All services exist
and have real Supabase repository calls. The only remaining work
is plugging in actual Claude API calls where marked // TODO:

  src/lib/claude/client.ts
    → Anthropic SDK wrapper (create Anthropic client, stream helper)

  src/services/profile.service.ts → createFromIntake()
    → Call Claude with buildProfilePrompt() to classify business type
    → Parse JSON response → set business_type, industry_sector, etc.

  src/services/roadmap.service.ts → generate()
    → Call Claude with buildRoadmapPrompt() + KB selector output
    → Parse JSON array → map to CreateRoadmapStepDTO[] → batchUpsert

  src/services/financial.service.ts → generate()
    → Tax calculator already done (pure math)
    → Call Claude with buildFinancialInsightPrompt() for watch_out_flags
    → Parse JSON → merge with tax calc results → upsert snapshot

  src/services/assistant.service.ts → chat()
    → Fetch profile + roadmap + funding + snapshot from repositories
    → Select KB docs with selectForAssistant()
    → Call Claude with buildAssistantPrompt() + full context
    → Save user + assistant messages to chat_messages table

  .env.local → ANTHROPIC_API_KEY
    → Still has placeholder value. Real key needed before Claude works.

----------------------------------------------------------------

OTHER PENDING ITEMS
--------------------

[ ] Google OAuth: enable in Supabase dashboard (Auth → Providers → Google)
[ ] Email confirmation: can be disabled in Supabase for hackathon speed
[ ] Seed demo account (Yara) with pre-built profile + roadmap + funding
[ ] Pre-populate response_cache for demo day offline safety
[ ] Add ANTHROPIC_API_KEY to .env.local (and Vercel env vars on deploy)

----------------------------------------------------------------

COMMIT REFERENCE
-----------------

Branch: feat/backend-logic
Commit: feat: complete backend architecture + intake wizard (clean build)
Files changed: 49 (16 new files created, 33 modified)

================================================================
END OF ENTRY — April 1, 2026
================================================================


================================================================
BUSINESS CLUSTER CLASSIFICATION — SESSION 3
================================================================

DATE: April 2, 2026
BRANCH: feat/backend-logic
BUILD STATUS: Zero TypeScript errors confirmed

----------------------------------------------------------------

OVERVIEW
--------
Added a full business cluster classification system. After the
intake wizard, every user gets assigned to one of 9 clusters
(C1-C9) based on their business description. The cluster drives
which KB files are loaded, which permits appear in the roadmap,
and what the assistant talks about. Designed so Claude makes the
classification call — currently defaulting to C9 until the API
key is wired in.

----------------------------------------------------------------

1. THE 9 CLUSTERS
------------------

src/lib/clusters.ts  [NEW FILE]

Defines the ClusterID type and the CLUSTERS lookup table.
Each entry has: label, complexity (low/medium/high), kb_files[].

  C1  Home-based food          — complexity: high
  C2  Freelance / consulting   — complexity: low
  C3  Regulated childcare      — complexity: high
  C4  Regulated professional   — complexity: medium
  C5  Retail / product sales   — complexity: medium
  C6  Food service/hospitality — complexity: high
  C7  Construction / trades    — complexity: high
  C8  Personal services        — complexity: medium
  C9  General / unknown        — complexity: medium  (default stub)

Each cluster has its own hand-picked list of KB JSON files so
Claude only sees the permits, taxes, and funding programs that
actually apply to that business type.

----------------------------------------------------------------

2. DATABASE MIGRATION
----------------------

Migration name: add_cluster_columns_to_profiles
Applied to: Supabase project ncpmghgxtaqhzohauymn

SQL applied:
  ALTER TABLE profiles
    ADD COLUMN IF NOT EXISTS cluster_id TEXT
      CHECK (cluster_id IN ('C1','C2','C3','C4','C5','C6','C7','C8','C9'))
      DEFAULT 'C9',
    ADD COLUMN IF NOT EXISTS cluster_label TEXT,
    ADD COLUMN IF NOT EXISTS cluster_complexity TEXT
      CHECK (cluster_complexity IN ('low','medium','high'));

All existing rows default to cluster_id = 'C9' automatically.

----------------------------------------------------------------

3. TYPE SYSTEM UPDATES
-----------------------

src/types/profile.ts
  - Added: export type ClusterID = 'C1'|'C2'|...|'C9'
  - Added: export type ClusterComplexity = 'low'|'medium'|'high'
  - Added 3 new fields to Profile interface:
      cluster_id: ClusterID | null
      cluster_label: string | null
      cluster_complexity: ClusterComplexity | null
  - CreateProfileDTO and UpdateProfileDTO pick these up
    automatically (they are Omit<Profile, ...> derived types).

----------------------------------------------------------------

4. CLAUDE LAYER
----------------

src/lib/claude/schemas.ts
  - Added ProfileResponseSchema (new):
      business_name: z.string().nullable()
      business_description: z.string().nullable()
      industry_sector: z.string().nullable()
      business_type: z.enum([...])
      cluster_id: z.enum(['C1',...,'C9'])
  - Added ProfileResponse type (inferred from schema).

src/lib/claude/prompts.ts
  - Added PROFILE_SYNTHESIS_PROMPT constant:
      Full system prompt for the intake classification call.
      Describes all 9 clusters with examples and rules.
      Rules: prefer C1 over C6 for home food, prefer C4 for
      professional orders, never return C9 if C1-C8 fits.
  - Added buildProfilePrompt(answers) function:
      Appends raw intake answers to PROFILE_SYNTHESIS_PROMPT.
  - Updated buildRoadmapPrompt(profile):
      If profile.cluster_id is set, injects at the top:
      "Business cluster: C1 — Home-based food (complexity: high)
       Only generate steps applicable to this cluster."
  - Updated buildSystemPrompt() [assistant prompt]:
      Same cluster context line injected before the user profile
      block so the chat assistant stays cluster-focused.

----------------------------------------------------------------

5. PROFILE SERVICE — CLUSTER WIRING
-------------------------------------

src/services/profile.service.ts
  - Imports CLUSTERS from @/lib/clusters and ClusterID type.
  - createFromIntake() now:
      1. Stubs: const clusterId: ClusterID = 'C9'
         (swap this one line for claudeResult.cluster_id when
          the API key is added)
      2. Reads clusterMeta = CLUSTERS[clusterId]
      3. Writes cluster_id, cluster_label, cluster_complexity
         into the CreateProfileDTO before upsert.
  - TODO comments in place for the 3 Claude lines to uncomment.

HOW TO ACTIVATE LIVE CLASSIFICATION:
  1. Add ANTHROPIC_API_KEY to .env.local
  2. Uncomment the 3 lines in createFromIntake()
  3. Replace `const clusterId = 'C9'` with `claudeClusterId`

----------------------------------------------------------------

6. KNOWLEDGE BASE SELECTOR — CLUSTER-FIRST LOGIC
--------------------------------------------------

src/lib/knowledge-base/selector.ts
  - Added import: CLUSTERS, ClusterID from @/lib/clusters
  - Added selectByClusterId(kb, clusterId) [NEW — primary]:
      Reads CLUSTERS[clusterId].kb_files, resolves each file key
      to a KB document via kb.getByKey(). Skips nulls.
      Use this instead of selectForPrompt() for all new code.
  - Added selectForClusterAssistant(kb, clusterId) [NEW]:
      Calls selectByClusterId(), then adds 3 universal extras
      (cra.json, installments.json, signage.json) for chat.
  - Old selectForPrompt() and selectForAssistant() kept for
    backwards compatibility, not deleted.

----------------------------------------------------------------

7. DASHBOARD — CLUSTER BADGE
------------------------------

src/app/(app)/dashboard/page.tsx
  - Converted from sync to async server component.
  - Now fetches profile via ProfileRepository.getByUserId().
  - Added CLUSTER_COLORS map (inline, no new file):
      low    → green badge
      medium → amber badge
      high   → red badge
  - Header now shows:
      - business_name (or "Dashboard" fallback)
      - business_description (or generic subtitle)
      - Cluster badge "C1 · Home-based food" in correct colour
        (only renders when cluster data exists on the profile)

----------------------------------------------------------------

8. DEMO PERSONA EXPECTED RESULTS (once API key is live)
---------------------------------------------------------

  Yara   — home pastry chef             → C1 (Home-based food)
  Marcus — freelance React developer    → C2 (Freelance / consulting)
  Fatima — licensed home daycare        → C3 (Regulated childcare)

  Currently all default to C9 (stub). One line swap activates.

----------------------------------------------------------------

FILES CHANGED IN THIS SESSION
-------------------------------

  src/lib/clusters.ts                        [NEW]
  src/types/profile.ts                       [+3 fields + 2 types]
  src/lib/claude/schemas.ts                  [+ProfileResponseSchema]
  src/lib/claude/prompts.ts                  [+synthesis prompt + cluster injection]
  src/services/profile.service.ts            [+cluster DTO fields]
  src/lib/knowledge-base/selector.ts         [+2 cluster selector functions]
  src/app/(app)/dashboard/page.tsx           [async + cluster badge]

DB migration applied:
  add_cluster_columns_to_profiles

================================================================
END OF ENTRY — April 2, 2026 (Clusters)
================================================================


================================================================
FINANCIAL CFO DASHBOARD — SESSION 4
================================================================

DATE: April 2, 2026
BRANCH: feat/backend-logic
BUILD STATUS: ✅ Zero TypeScript errors

----------------------------------------------------------------

OVERVIEW
--------
Built a complete "personal CFO dashboard" for micro-business owners.
The page answers three questions at once:
  1. Where am I now?  → The snapshot (hero take-home + waterfall bar)
  2. Where am I going? → 12-month projections, scenarios, break-even
  3. What should I do?  → Watch-out flags, deductions, funding impact

All calculations are deterministic pure math — no API calls needed.
Every number updates live as the user adjusts inputs. Claude is
only involved for future watch-out flags (currently deterministic).

Cluster-aware: all expense defaults, deduction suggestions, and
financial hints are pre-loaded based on the user's business cluster
(C1-C9), not just a flat business_type string.

Progressive disclosure: Tier 1 (intake data) shows a useful
snapshot immediately. Tier 2 (expenses) is pre-filled by cluster
and editable. Tier 3 (pricing, deductions) is expandable sections.

----------------------------------------------------------------

1. NEW BACKEND ENGINE FILES
-----------------------------

src/lib/financial/expense-defaults.ts  [NEW]
  - Cluster-aware expense defaults with full category breakdowns
  - Each of 9 clusters has its own list of ExpenseCategory objects:
      { key, label, amount, description }
  - C1 (home food): 6 categories — ingredients $250, packaging $80,
    market fees $60, delivery $45, insurance $30, permits $15 = $480
  - C2 (freelance): 5 categories — software $120, home office $200,
    phone $45, professional dev $30, accounting $25 = $420
  - C3 (childcare): 6 categories — meals $180, supplies $80,
    safety $20, insurance $55, cleaning $35, training $15 = $385
  - C4 (regulated prof): 5 categories — dues $125, E&O insurance $150,
    office $300, software $80, CE credits $60 = $715
  - C5 (retail): 5 categories — inventory $400, shipping $80,
    platform fees $50, marketing $60, insurance $35 = $625
  - C6 (food service): 6 categories — food cost $800, rent $1200,
    utilities $200, equipment $150, insurance $100, permits $40 = $2490
  - C7 (construction): 6 categories — materials $500, vehicle $350,
    tools $120, insurance $180, RBQ $45, phone $40 = $1235
  - C8 (personal services): 5 categories — products $150,
    chair/room $300, insurance $45, tools $40, marketing $30 = $565
  - C9 (general): 4 categories — general $100, phone $45,
    software $40, insurance $30 = $215
  - Exports: getExpenseDefaults(clusterId), getDefaultExpenseTotal(businessType)

src/lib/financial/projections.ts  [NEW]
  - Scenario comparison engine:
      generateScenarios(revenue, expenses, structure)
      Returns 3 scenarios: conservative (70%), expected (100%), optimistic (140%)
      Each has: taxes, take-home, keep-rate, GST-required flag
  - Break-even calculator:
      calculateBreakEven(expenses, structure) — binary search because
      tax brackets make break-even non-linear
      getBreakEvenResult() — adds surplus, months-to-break-even
      (assumes 10% monthly growth), isAboveBreakEven flag
  - Pricing calculator:
      calculatePricing(pricePerUnit, unitsPerMonth, expenses, structure)
      Returns: gross, take-home, per-unit take-home, per-unit tax burden
  - Monthly projection data:
      generateMonthlyProjections() — 12-month cumulative with GST threshold crossing
  - Funding impact calculator:
      calculateFundingImpact(fundingAmount, expenses, takeHome)
      Returns: months of runway, months of full income, readable message

src/lib/financial/deductions.ts  [NEW]
  - Cluster-aware common deductions with estimated tax savings
  - Each cluster has its own list of Deduction objects:
      { key, label, annualAmount, description, source }
  - Source field references the actual CRA form (T2125, CCA Schedule, etc.)
  - Tax savings estimated at ~27% combined marginal rate (federal + QC)
  - C1 example: home kitchen $1200 + ingredients $3000 + packaging $960
    + mileage $1728 + market fees $720 + MAPAQ $180 = $7,788 total
    → saves ~$2,103/year in taxes
  - Exports: getDeductionSummary(clusterId)

----------------------------------------------------------------

2. TYPE SYSTEM UPDATES
-----------------------

src/types/financial.ts
  - Added re-exports for all new types:
      Scenario, BreakEvenResult, PricingResult, FundingImpact,
      MonthProjection, ExpenseCategory, ClusterExpenseProfile,
      Deduction, DeductionSummary

----------------------------------------------------------------

3. SERVICE LAYER UPDATES
--------------------------

src/services/financial.service.ts
  - Added imports for all 3 new engine modules
  - Added 6 new methods to FinancialService:
      getExpenseDefaults(clusterId)  → cluster expense categories
      getDeductionSummary(clusterId) → deductions + tax savings
      getScenarios(revenue, expenses, structure) → 3 scenarios
      getBreakEven(revenue, expenses, structure) → break-even point
      getPricing(price, units, expenses, structure) → pricing calc
      getFundingImpact(funding, expenses, takeHome) → runway calc
  - All existing methods (generate, getByProfileId, refresh) unchanged

----------------------------------------------------------------

4. SNAPSHOT CARD UPDATE
------------------------

src/components/financial/snapshot-card.tsx
  - Replaced hardcoded EXPENSE_DEFAULTS map with cluster-aware:
      import { getExpenseDefaults } from '@/lib/financial/expense-defaults'
      const clusterId = (profile?.cluster_id ?? 'C9') as ClusterID
      const monthlyExpenses = getExpenseDefaults(clusterId).total
  - Now shows accurate cluster-specific expense totals on dashboard

----------------------------------------------------------------

5. FINANCIAL PAGE — FULL REWRITE
----------------------------------

src/app/(app)/financial/page.tsx  [FULL REWRITE — 650+ lines]

PRESERVED from previous version (my friend's UI):
  - WaterfallBar component (horizontal stacked bar)
  - ProjectionChart (12-month SVG line chart with GST threshold)
  - TaxCalendar (timeline with installment dates/amounts)
  - QPPShock (double contribution explainer)
  - generateFlags() (deterministic watch-out flags)
  - Hero card (gradient teal, big take-home number)
  - 4 stat cards (Revenue, Tax, Expenses, Take-Home)
  - Input section (revenue + expenses)

NEW COMPONENTS ADDED:

  Expandable — reusable accordion wrapper (icon, title, subtitle, children)
    Used for: expense breakdown, tax breakdown, scenarios, pricing, calendar, deductions

  ExpenseDonut — SVG donut chart showing expense categories
    - Teal/amber colour palette, 9 segment colours
    - Center shows total expense amount
    - Legend shows each category with amount
    - Categories are pre-loaded from cluster defaults

  PricingSlider — "If I charge $X and sell Y, what do I keep?"
    - Price per unit slider ($5-$500)
    - Units per month slider (1-200)
    - 3 result cards: Gross Revenue, You Keep, Per Unit Profit
    - Dynamic hint: "Charging $5 more adds ~$XXX/month to take-home"

  ScenarioComparison — 3 side-by-side revenue scenarios
    - Conservative (70%), Expected (100%), Optimistic (140%)
    - Each shows: take-home, tax burden, keep rate, GST required
    - Expected scenario has a teal highlight ring

  BreakEvenCard — visual break-even indicator
    - Shows minimum revenue needed vs current projection
    - Green/amber/red progress bar
    - If below: "At 10% monthly growth, reach break-even in ~N months"
    - If above: "You're $X/mo above break-even — healthy margin"

  DeductionTracker — cluster-aware deduction list
    - Green savings headline: "Estimated annual tax savings: $X,XXX"
    - Each deduction shows: label, description, CRA source, annual amount
    - Per-deduction savings shown (amount × 27%)

  FundingImpactCard — how grants extend runway
    - 3 stat boxes: funding available, months of expenses, months of full income
    - Human-readable message about runway
    - Link to funding matches page

PAGE STRUCTURE (top to bottom):

  ZONE 1: The Snapshot (always visible)
    → Hero card with take-home number + waterfall
    → Revenue + expense inputs
    → 4 stat cards
    → Expense donut (expandable)
    → Tax breakdown (expandable)
    → QPP shock explainer

  ZONE 2: Where Am I Going (projections)
    → 12-month projection chart
    → Revenue scenarios (expandable, default open)
    → Break-even indicator
    → Pricing calculator (expandable)

  ZONE 3: Tax Calendar & Deductions
    → 2026 tax calendar (expandable)
    → Common deductions tracker (expandable)

  ZONE 4: What Should I Do (action items)
    → Watch-out flags
    → Funding impact card

EXPENSE HANDLING:
  - Old: flat EXPENSE_DEFAULTS[businessType] → single number
  - New: getExpenseDefaults(clusterId) → category array, each editable inline
  - Donut chart updates live as user changes individual category amounts
  - Total expense feeds into all calculations

----------------------------------------------------------------

FILES CHANGED IN THIS SESSION
-------------------------------

  src/lib/financial/expense-defaults.ts     [NEW — 165 lines]
  src/lib/financial/projections.ts          [NEW — 186 lines]
  src/lib/financial/deductions.ts           [NEW — 188 lines]
  src/types/financial.ts                    [+re-exports]
  src/services/financial.service.ts         [+6 methods]
  src/components/financial/snapshot-card.tsx [cluster-aware expenses]
  src/app/(app)/financial/page.tsx          [FULL REWRITE — 650+ lines]

================================================================
END OF ENTRY — April 2, 2026 (Financial Dashboard)
================================================================
