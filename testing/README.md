# Testing Structure

All tests live under the `testing/` directory.

- `testing/unit/` - unit tests for deterministic logic
- `testing/integration/` - API/service integration tests (add next)
- `testing/e2e/` - full user-flow tests (add next)

Current coverage:

- `testing/unit/financial/tax-calculator.test.ts` - deterministic tax calculator behavior
